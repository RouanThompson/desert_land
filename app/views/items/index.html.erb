<!--Items Index Page-->
<div class="form">
    <p>Desert Categories</p>

    <%Category.all.each do |category|%>
        <p><%= link_to category.name, category, class: "link" %></p>
    <%end%>
</div>