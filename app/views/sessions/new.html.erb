<!DOCTYPE html>
<html>
    <div class="user">
        <header class="user__header">
            <img src="https://s3-us-west-2.amazonaws.com/s.cdpn.io/3219/logo.svg" alt="" />
            <h1 class="user__title", style="color:pink;text-align:center;">Login</h1>
        </header>
        
        <br>
        <br>
        <br>
        <%= link_to "Sign Up", signup_path%>
        <%= form_with scope: :session, url: '/login', html: {class: "form"} do |f|%>

            <p style="color:orange;text-align:center;" ><%= f.label :username%>:</p>
            <p style="color:orange;text-align:center;" ><%= f.text_field :username%><br></p>

            <p style="color:orange;text-align:center;" ><%= f.label :password%>:</p>
            <p style="color:orange;text-align:center;" ><%= f.text_field :password%><br></p>

            <p style="color:green;text-align:center;" ><%= f.submit "Login" %></p>
            <%if @errors%>
                <% @errors.each do |error|%>
                    <p style="color:red;"><%= error %></p>
                <% end %>
            <% end %>
        <% end %>
    </div>
</html>